export enum BotStatus {
    EVENT_RECEIVED = 'EVENT_RECEIVED',
}

export enum ResponseCode {
    OK = 200,
    BAD_REQUEST = 400,
    UNAUTHORIZED = 401,
    NOT_FOUND = 404,
    SERVER_ERROR = 500,
};
